#include<stdio.h>
void main()
{
    printf("%d",printf("Pankaj\n"));// int printf(*s .arg1,arg2,.....) -printf() always returns an integer value which is equal to the n0. of characters inside string s to be printed
}
